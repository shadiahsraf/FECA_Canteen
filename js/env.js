/* env.js — populated at build time from Vercel environment variables.
 * By default it is empty, so config.js falls back to the values you set there.
 * See README → "Option B: Vercel environment variables". */
window.ENV = window.ENV || {};
